---
const { onClick, id, style, type, icon } = Astro.props;
---

<button
  id={id}
  type={type}
  class={`${style} ${id}  px-3 py-2 inline-flex items-center gap-2 bg-primary-100 hover:text-white hover:bg-primary-600 focus:ring-primary-600 text-white rounded text-sm hover:bg-primary/90 transition-colors`}
>
  <slot />
</button>
