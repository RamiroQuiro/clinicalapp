---
const { onClick, id, style, type, icon } = Astro.props;
---

<button
  id={id}
  type={type}
  class={`${style} ${id}  px-2 md:px-3 py-1 md:py-2 inline-flex items-center gap-2 bg-primary-100 text-white hover:bg-primary-100/90 focus:ring-primary-100 rounded text-xs md:text-sm hover:bg-primary/90 transition-colors`}
>
  <slot />
</button>
