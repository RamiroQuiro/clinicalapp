---
<<<<<<< HEAD
import Button1 from "../atomos/Button1.astro";
import ContenedorTitleBotonera from "../atomos/ContenedorTitleBotonera.astro";
import DivBox1 from "../atomos/DivBox1.astro";

interface Visitas {
  fecha: string;
  motivo: string;
  doctor?: string;
  department?: string;
  tratamiento?:string;
  diagnosticos?:string
}

interface RecentVisitsProps {
  visitas: Visitas[];
}

const { visitas } = Astro.props as RecentVisitsProps;

const cabeceraColumn = [
  {
    id: 1,
    label: "fecha",
  },
  {
    id: 2,
    label: "motivo",
  },
  {
    id: 3,
    label: "diagnostico",
  },
  {
    id: 4,
    label: "tratemiento",
  },
];
---

<DivBox1>
  <ContenedorTitleBotonera>
    <h2 class="text-lg font-semibold">Visitas Recientes</h2>
    <div>
      <Button1>ver historial</Button1>
    </div>
  </ContenedorTitleBotonera>

  <div
    class="overflow-x-auto w-full bg-primary-bg-componentes rounded-lg border"
  >
    <table class="w-full">
      <thead class="w-full">
        <tr class="text-left border-b">
          {
            cabeceraColumn?.map((colum) => (
              <th class="py-3 pl-2 text-sm font-medium capitalize">
                {colum.label}
              </th>
            ))
          }
        </tr>
      </thead>
      <tbody>
        {
          visitas.map((visit, index) => (
            <tr class="border-b last:border-0 bg-white">
              <td class=" text-xs  px-2 py-2.5">{visit.fecha}</td>
              <td class=" text-xs  px-2 py-2.5">{visit.motivo}</td>
              <td class=" text-xs  px-2 py-2.5">{visit.diagnosticos}</td>
              <td class=" text-xs  px-2 py-2.5">
                <span class="px-2 py-1 text-xs font-medium rounded-full bg-primary text-primary-foreground">
                  {visit.tratamiento}
                </span>
              </td>
            </tr>
          ))
        }
      </tbody>
    </table>
  </div>
</DivBox1>
=======

interface Visit {
  date: string;
  reason: string;
  doctor: string;
  department: string;
}

interface RecentVisitsProps {
  visits: Visit[];
}

const {visits}=Astro.props as RecentVisitsProps
---
<div class="bg-white rounded-lg shadow-sm p-6 mb-6">
    <div class="flex justify-between items-center mb-4">
      <h2 class="text-lg font-semibold">Visitas Recientes</h2>
      <button class="text-sm text-primary-foreground hover:text-primary-foreground/80">
        Ver todas
      </button>
    </div>
    
    <div class="overflow-x-auto">
      <table class="w-full">
        <thead>
          <tr class="text-left border-b">
            <th class="pb-3 text-sm font-medium text-muted-foreground">Fecha</th>
            <th class="pb-3 text-sm font-medium text-muted-foreground">Motivo</th>
            <th class="pb-3 text-sm font-medium text-muted-foreground">Doctor</th>
            <th class="pb-3 text-sm font-medium text-muted-foreground">Departamento</th>
          </tr>
        </thead>
        <tbody>
          {visits.map((visit, index) => (
            <tr key={index} class="border-b last:border-0">
              <td class="py-4 text-sm">{visit.date}</td>
              <td class="py-4 text-sm">{visit.reason}</td>
              <td class="py-4 text-sm">{visit.doctor}</td>
              <td class="py-4 text-sm">
                <span class="px-2 py-1 text-xs font-medium rounded-full bg-primary text-primary-foreground">
                  {visit.department}
                </span>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </div>
>>>>>>> d01c59ff6fdf92946889071885401a6b84e0991d
